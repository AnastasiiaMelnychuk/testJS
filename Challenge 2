function sum_even_numbers() {
  var sum = [];
  var result = 0;
  for (i = 1; i <= 1000; i++) {
    if (i % 2 == 0){
      sum.push(i);
    }
  }
  for (let n in sum) {
    result += sum[n];
  }
  return result;
}
sum_even_numbers();
